data/
.env
__pycache__/
venv/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/
# Orphaned root-owned remnant of the pre-refactor layout (model artifacts now
# live in ./models/). Left on disk because it's root-owned; remove with sudo.
src/analytics_engine/

# Local backups (use an off-machine copy for real durability)
backups/
