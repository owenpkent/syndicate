[package]
name = "book"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "L2 order-book reconstruction from snapshot + sequenced deltas, with gap detection."

[dependencies]

[dev-dependencies]
proptest = "1"
