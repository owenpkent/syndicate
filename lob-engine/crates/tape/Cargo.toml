[package]
name = "tape"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Normalized append-only binary market-data tape (order-book snapshots + trades)."

[dependencies]
