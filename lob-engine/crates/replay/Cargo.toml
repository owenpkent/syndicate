[package]
name = "replay"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Replay a tape through sim-time into the order book — the trading-sim's game clock."

[dependencies]
tape = { path = "../tape" }
book = { path = "../book" }
