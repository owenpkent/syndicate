"""Long-running micro-agents that compose the pipeline.

Each module exposes a ``main()`` entrypoint (wired as a console script in
pyproject.toml) and is otherwise import-safe so it can be unit tested.
"""
